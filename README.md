An experimental fork where the MateEngine reads the window title (on Windows) and sends it to the LLM to get some random comments.

Risk: the window titles are filtered to prevent sensitive information from being sent to the (cloud) LLM, but the rules may not cover all cases.

